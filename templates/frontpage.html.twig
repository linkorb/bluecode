{% extends "@Theme/layout.html.twig" %}

{% block content %}
    <h1>Frontpage</h1>
    
    <a href="{{ path("projects_index" )}}" class="btn btn-lg btn-primary">View all the projects!</a>
    
{% endblock %}
